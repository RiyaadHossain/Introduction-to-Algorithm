#include <bits/stdc++.h>
using namespace std;


bool interleavingString(string s1, string s2, string s3) {

}

int main()
{
    string s1, s2, s3;
    cin >> s1 >> s2 >> s3;

    bool result = interleavingString(s1, s2, s3);
    cout << (result ? "YES" : "NO");

    return 0;
}